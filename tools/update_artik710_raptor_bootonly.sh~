#!/bin/bash

set -e

PARENT_DIR="${PWD%/*}"
META_NEXELL_TOOLS_DIR="${PARENT_DIR}/meta-nexell/tools"

PARTMAP="${META_NEXELL_TOOLS_DIR}/partmap_emmc.txt"

fastboot flash partmap ${PARTMAP}
fastboot flash boot boot.img
